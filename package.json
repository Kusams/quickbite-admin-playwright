{
  "name": "quickbite-admin-playwright",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test --reporter=list,html",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
